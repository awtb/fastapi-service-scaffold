.PHONY: install run test{% if include_pre_commit %} hooks pre-commit{% endif %}

install:
	uv sync --extra dev

run: install
	uv run python -m {{ package_name }} --reload

test: install
	uv run pytest
{% if include_pre_commit %}

hooks: install
	uv run pre-commit install

pre-commit: install
	uv run pre-commit run --all-files
{% endif %}
